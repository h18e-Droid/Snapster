"use client"
import { Pagination } from "@/shared/ui/pagination"

const Profile = () => {
  return (
    <div>
      <Pagination totalSize={1232} onChange={() => {}} />
    </div>
  )
}

export default Profile
